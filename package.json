{
  "name": "desinfect",
  "version": "0.1.0",
  "private": true,
  "description": "Node foundation for Cloudflare and browser tooling.",
  "license": "CC0-1.0",
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11 <12"
  },
  "scripts": {
    "test": "node --test tests/node/*.test.mjs tests/web/*.spec.mjs",
    "test:foundation": "node --test tests/node/foundation.test.mjs",
    "test:web": "node --test tests/web/*.spec.mjs"
  }
}
